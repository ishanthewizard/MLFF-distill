defaults: 
 - base_dataset
 - _self_
 
omol_train:
  splits:
    train:
      src: ${cluster.omol_path}/4M/subsets/OMol_subset/protein_ligand_fragments_train_4M
omol_val:
  splits:
    val:
      sample_n: 2000
      src: ${cluster.omol_path}/4M/subsets/OMol_subset/protein_ligand_fragments_val
