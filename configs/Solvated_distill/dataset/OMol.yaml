# omol_path_germain: /data/ishan-amin/OMOL
teacher_labels_folder: /home/ishan-amin/MLFF-distill/data/labels/SPICE_labels/EScAIP_SpiceAminos

omol_train:
  splits:
    train:
      src: /data/ishan-amin/OMOL/TOY/solvated/train
  format: src_v2.distill_datasets.CombinedDatasetTrain
  teacher_labels_folder: data/labels/SPICE_labels/EScAIP_SpiceAminos #${teacher_labels_folder}
  num_hessian_samples: 1
  a2g_args:
    # molecule_cell_size: 120.0
    r_energy: True
    r_forces: True
    # r_data_keys: ['spin', 'charge']
    r_edges: ${cpu_graph}
    radius: ${cutoff_radius}
    max_neigh: ${max_neighbors}
  key_mapping:
    energy: omol_energy
    forces: ${omol_forces_key}
  transforms:
    common_transform:
      dataset_name: omol

omol_val:
  splits:
    val:
      src: /data/ishan-amin/OMOL/TOY/solvated/val
  format: src_v2.distill_datasets.CombinedDatasetVal
  teacher_labels_folder: data/labels/SPICE_labels/EScAIP_SpiceAminos #${teacher_labels_folder}
  a2g_args:
    # molecule_cell_size: 120.0
    r_energy: True
    r_forces: True
    # r_data_keys: ['spin', 'charge']
    r_edges: ${cpu_graph}
    radius: ${cutoff_radius}
    max_neigh: ${max_neighbors}
  key_mapping:
    energy: omol_energy
    forces: ${omol_forces_key}
  transforms:
    common_transform:
      dataset_name: omol
