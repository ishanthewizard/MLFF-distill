normalizer_rmsd: 0.8290896948547554
regress_stress: true
dataset_name: omol

train_dataset:
  splits:
    train:
      src: ${cluster.omol_path}/ablation_lmdbs_323K/napf6_sample_20fs_unequil/train
  format: ase_db
  a2g_args:
    r_energy: True
    r_forces: True
    r_data_keys: ['spin', 'charge']
  no_shuffle: true
  transforms:
    common_transform:
      dataset_name: ${dataset.dataset_name}
    stress_reshape_transform:
      dataset_name: ${dataset.dataset_name}
  teacher_labels_folder: ${cluster.omol_path}/ablation_lmdbs_323K/napf6_sample_20fs_unequil/val

val_dataset:
  splits:
    val:
      src: ${cluster.omol_path}/ablation_lmdbs_323K/napf6_unequilibrated/val
  format: ase_db
  a2g_args:
    r_energy: True
    r_forces: True
    r_data_keys: ['spin', 'charge']
  no_shuffle: true
  transforms:
    common_transform:
      dataset_name: ${dataset.dataset_name}
    stress_reshape_transform:
      dataset_name: ${dataset.dataset_name}
  teacher_labels_folder: ${cluster.omol_path}/electrolytes_application/labels/NAPF6/s1p1
