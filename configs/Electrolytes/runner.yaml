defaults: 
  - distill # OTHER OPTIONS: 1. train 2. distill 3. eval 4. labels
  - override cluster: delta
  # - override backbone: eSEN_xtra_small  #Not needed for eval
  - override dataset: all_NAPF6 #Solvated
  - override element_refs: all_NAPF6_refs
  - _self_

# batch_size: 1
max_atoms: 3000
job:
  run_name: all_NAPF6_distill_hessian_coef_80
  # graph_parallel_group_size: 1

# runner:
#   train_eval_unit:
#     cosine_lr_scheduler_fn:
#       warmup_epochs: 0.1 # usually 0.01 but solvated and protein_ligand are so small if you use 0.01 you get div by 0 error

# direct_forces_coef: 30
# energy_coef:        30
hessian_coef:       80
# num_hessian_samples: 3
