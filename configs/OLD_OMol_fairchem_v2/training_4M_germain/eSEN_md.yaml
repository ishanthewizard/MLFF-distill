defaults:
  - cluster: ada6000_local
  # - cluster: fair_cluster
  # - backbone: eSEN
  - dataset: OMol
  - element_refs: eSEN_omol
  - tasks: eSEN_direct
  - _self_

job:
  device_type: ${cluster.device}
  scheduler:
    mode: ${cluster.mode}
    ranks_per_node: ${cluster.ranks_per_node}
    num_nodes: 1
    slurm:
      qos: ${cluster.qos}
      partition: ${cluster.partition}
      nodelist: ${cluster.nodelist}
      mem_gb: ${cluster.mem_gb}
      timeout_hr: ${cluster.timeout_hr}
      cpus_per_task: ${cluster.cpus_per_task}
  debug: ${cluster.debug}
  run_dir: ${cluster.run_dir}
  run_name: eSEN_label
  logger:
    _target_: fairchem.core.common.logger.WandBSingletonLogger.init_wandb
    _partial_: true
    entity: jianyuelalala # your account name
<<<<<<<< HEAD:configs_yj/OMol_label/training_4M_germain/eSEN_md.yaml
    project: distill_labels
========
    project: distill
>>>>>>>> fairchemV2:configs/OLD_OMol_fairchem_v2/training_4M_germain/eSEN_md.yaml

starting_checkpoint: /home/yuejian/project/MLFF-distill/OMol_Whole/ckpt/esen_sm_direct_all.pt

# moe_layer_type: pytorch
# num_moe_experts: 32
max_neighbors: 30
cutoff_radius: 6
epochs: null
steps: 2080000 # 80B atoms, 128 ranks, max atoms 350 (mean atoms 300)
max_atoms: 350 # important
bf16: True
cpu_graph: True
# otf_graph: False
normalizer_rmsd: 1.433569
# direct_forces_coef: 30
energy_coef: 10
# omc_energy_coef: 10
# omol_energy_coef: 30
# odac_energy_coef: 10
# oc20_energy_coef: 10
# omat_energy_coef: 10

# regress_stress: False
# direct_forces: True

# oc20_forces_key: forces
# omat_forces_key: forces
omol_forces_key: forces
# odac_forces_key: forces
# omc_forces_key: forces

# dataset_list: ["oc20", "omol", "omat", "odac", "omc"]

exclude_keys: [
  "id", # only oc20,oc22 have this
  "fid", # only oc20,oc22 have this
  "absolute_idx", # only ani has this
  "target_pos", # only ani has this
  "ref_energy", # only ani/geom have this
  "pbc", # only ani/transition1x have this
  "nads", # oc22
  "oc22", # oc22
  "formation_energy", # spice
  "total_charge", # spice
]

train_dataset:
  _target_: fairchem.core.datasets.ase_datasets.AseDBDataset
  config: ${dataset.omol_train}



val_dataset:
  _target_: fairchem.core.datasets.ase_datasets.AseDBDataset
  config: ${dataset.omol_val}


train_dataloader:
  _target_: fairchem.core.components.common.dataloader_builder.get_dataloader
  dataset: ${train_dataset}
  batch_sampler_fn:
    _target_: fairchem.core.datasets.samplers.max_atom_distributed_sampler.MaxAtomDistributedBatchSampler
    _partial_: True
    max_atoms: ${max_atoms}
    shuffle: False
    seed: 0
  num_workers: ${cluster.dataloader_workers}
  collate_fn:
    _target_: fairchem.core.units.mlip_unit.mlip_unit.mt_collater_adapter
    tasks: ${tasks}
    exclude_keys: ${exclude_keys}

eval_dataloader:
  _target_: fairchem.core.components.common.dataloader_builder.get_dataloader
  dataset: ${val_dataset}
  batch_sampler_fn:
    _target_: fairchem.core.datasets.samplers.max_atom_distributed_sampler.MaxAtomDistributedBatchSampler
    _partial_: True
    max_atoms: ${max_atoms}
    shuffle: False
    seed: 0
  num_workers: ${cluster.dataloader_workers}
  collate_fn:
    _target_: fairchem.core.units.mlip_unit.mlip_unit.mt_collater_adapter
    tasks: ${tasks}
    exclude_keys: ${exclude_keys}

heads:
  energy:
    module: fairchem.core.models.uma.escn_md.MLP_Energy_Head
  forces:
    module: fairchem.core.models.uma.escn_md.Linear_Force_Head

runner:
  _target_: src.labels_trainer.TeacherLabelGenerator
  # _target_: fairchem.core.components.train.train_runner.TrainEvalRunner
  train_dataloader: ${train_dataloader}
  eval_dataloader: ${eval_dataloader}
  train_eval_unit:
    _target_: fairchem.core.units.mlip_unit.mlip_unit.MLIPTrainEvalUnit
    job_config: ${job}
    tasks: ${tasks}
    model:
      _target_: src.dataset_utils.initialize_finetuning_model
      checkpoint_location: ${starting_checkpoint}
      # overrides: None
      heads: ${heads}
    optimizer_fn:
      _target_: torch.optim.AdamW
      _partial_: true
      lr: 8e-4
      weight_decay: 1e-3
    cosine_lr_scheduler_fn:
      _target_: fairchem.core.units.mlip_unit.mlip_unit._get_consine_lr_scheduler
      _partial_: true
      warmup_factor: 0.2
      warmup_epochs: 0.01
      lr_min_factor: 0.01
      epochs: ${epochs}
      steps: ${steps}
    print_every: 10
    clip_grad_norm: 100
    bf16: ${bf16}
  label_folder: ${dataset.omol_train.src}/labels
