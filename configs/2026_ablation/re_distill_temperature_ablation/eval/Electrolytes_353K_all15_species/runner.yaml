defaults: 
  - eval # OTHER OPTIONS: 1. train 2. distill 3. eval 4. labels
  - override cluster: local
  # - override backbone: eSEN_xtra_XTRA_small  #Not needed for eval
  - override dataset: ALL_15_353 #Solvated
  - override element_refs: 353K
  - _self_

# batch_size: 1
max_atoms: 3000
job:
  run_name: all15_species_323K_wo_hessian
  # graph_parallel_group_size: 1

# runner:
#   train_eval_unit:
#     cosine_lr_scheduler_fn:
#       warmup_epochs: 0.1 # usually 0.01 but solvated and protein_ligand are so small if you use 0.01 you get div by 0 error

# direct_forces_coef: 30
# energy_coef:        30
# hessian_coef:       60
# num_hessian_samples: 3
